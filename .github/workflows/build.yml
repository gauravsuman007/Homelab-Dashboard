# Build and publish the multi-arch image on every push.
#
# Four platforms are built into one manifest list, so `docker pull` picks the
# right one automatically on a NAS, a Pi, an old 32-bit box, or a normal server.
# Only amd64 runs natively on the runner; the rest go through QEMU, which is why
# the layer cache matters -- an emulated PyYAML compile is the slow part.
#
# GHCR needs no secrets (GITHUB_TOKEN is enough). Docker Hub is published only
# when DOCKERHUB_USERNAME/DOCKERHUB_TOKEN exist, so a fork or a clone without
# those secrets still gets a working build instead of a red run.
name: build

on:
  push:
    branches: ['**']
    tags: ['v*']
  pull_request:
  workflow_dispatch:

env:
  GHCR_IMAGE: ghcr.io/${{ github.repository_owner }}/homelab-dashboard
  DOCKERHUB_IMAGE: ${{ github.repository_owner }}/homelab-dashboard
  PLATFORMS: linux/amd64,linux/386,linux/arm/v7,linux/arm64

jobs:
  # Cheap gate: catch a syntax error or a bad import in seconds, before paying
  # for four emulated image builds.
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Compile all sources
        run: python -m compileall -q app

      - name: Import the app and exercise discovery without a docker socket
        # The modules must import and the pure functions must work on a machine
        # with no Docker at all -- that is what keeps discovery unit-testable.
        run: |
          cd app
          python - <<'PY'
          import discovery
          import stats

          # NPM config parsing, straight from a representative server block.
          conf = {
              "1.conf": '''
                server {
                  set $forward_scheme http;
                  set $server         "192.168.2.100";
                  set $port           8080;
                  listen 443 ssl;
                  server_name zvg.example.com zvg.example.de;
                  ssl_certificate /etc/letsencrypt/live/npm-1/fullchain.pem;
                }
              ''',
              "2.conf": '''
                server {
                  set $server "someapp";
                  set $port 5800;
                  server_name plain.example.com;
                }
              ''',
          }
          hosts = discovery.parse_proxy_hosts(conf)
          assert len(hosts) == 3, hosts
          assert {h.domain for h in hosts} == {
              "zvg.example.com", "zvg.example.de", "plain.example.com"}
          https = {h.domain: h.https for h in hosts}
          assert https["zvg.example.com"] is True, "cert present means https"
          assert https["plain.example.com"] is False, "no cert means http"

          # Host-IP inference: two or more matching published ports means local,
          # one is a coincidence and must be rejected.
          ips = discovery.infer_host_ips(
              proxy_hosts=[
                  discovery.ProxyHost("a.example.com", "192.168.2.100", 8080, True),
                  discovery.ProxyHost("b.example.com", "192.168.2.100", 7878, True),
                  discovery.ProxyHost("c.example.com", "192.168.2.39", 5001, True),
              ],
              published_ports={8080, 7878, 5001},
              gateways={"172.18.0.1"},
              seen_hosts=set(),
              configured=set(),
          )
          assert "192.168.2.100" in ips, ips
          assert "192.168.2.39" not in ips, "single-port match is not this box"

          # Names and categories must derive from daemon metadata alone, since
          # apps.yml is optional and most users will never write one.
          def facts(**kw):
              base = dict(name="c", image="", running=True, host_ports=[], internal_ports=[],
                          aliases=set(), labels={}, host_network=False)
              return discovery.ContainerFacts(**{**base, **kw})

          # The image author's title wins...
          assert discovery.display_name(facts(oci_title="Radarr", image="lscr.io/x/radarr:latest")) == "Radarr"
          # ...unless it is really just a pull reference (hotio ships these).
          assert discovery.display_name(
              facts(oci_title="hotio/whisparr:v3", image="ghcr.io/hotio/whisparr:v3")) == "Whisparr"
          # Image name beats a compose service named for its role in a stack.
          assert discovery.display_name(
              facts(name="wordpress_gaurav", image="wordpress:latest", compose_service="wp")) == "Wordpress"
          # Existing capitalisation is never destroyed.
          assert discovery.display_name(facts(oci_title="qBittorrent")) == "qBittorrent"

          assert discovery.categorise(facts(name="radarr", image="lscr.io/x/radarr")) == "Automation"
          assert discovery.categorise(facts(name="jellyfin", image="jellyfin/jellyfin")) == "Media"
          assert discovery.categorise(facts(name="mystery", image="me/mystery")) == "Other"

          # Two containers of one image must not produce two identical cards.
          pair = [
              discovery.App(key="decypharr_local", name="Decypharr", icon="x", category="Downloads", image="i", running=True),
              discovery.App(key="decypharr_torbox", name="Decypharr", icon="x", category="Downloads", image="i", running=True),
          ]
          discovery._disambiguate(pair, set())
          assert {a.name for a in pair} == {"Decypharr (Local)", "Decypharr (Torbox)"}, [a.name for a in pair]

          # Traefik and Caddy keep their routing table in labels on the
          # proxied containers, so these parsers stand in for a whole proxy.
          traefik = discovery.parse_traefik_labels([
              facts(name="grafana", internal_ports=[3000], labels={
                  "traefik.enable": "true",
                  "traefik.http.routers.graf.rule": "Host(`graf.example.com`)",
                  "traefik.http.routers.graf.entrypoints": "websecure",
                  "traefik.http.routers.graf.tls.certresolver": "le",
                  "traefik.http.services.graf.loadbalancer.server.port": "3000",
              }),
              # Several hosts in one rule, plain HTTP, and the port left to the
              # container's single exposed one.
              facts(name="wiki", internal_ports=[8080], labels={
                  "traefik.http.routers.w.rule": "Host(`a.example.com`, `b.example.com`) && PathPrefix(`/x`)",
              }),
              facts(name="secret", internal_ports=[9000], labels={
                  "traefik.enable": "false",
                  "traefik.http.routers.s.rule": "Host(`no.example.com`)",
              }),
          ])
          got = {(h.domain, h.upstream_host, h.upstream_port, h.https) for h in traefik}
          assert got == {
              ("graf.example.com", "grafana", 3000, True),
              ("a.example.com", "wiki", 8080, False),
              ("b.example.com", "wiki", 8080, False),
          }, got

          # A wildcard or regexp matcher has no single address to link to.
          assert discovery._host_rule_domains("Host(`*.example.com`)") == []
          assert discovery._host_rule_domains("PathPrefix(`/only`)") == []

          caddy = discovery.parse_caddy_labels([
              facts(name="app", internal_ports=[5000], labels={
                  "caddy": "app.example.com",
                  "caddy.reverse_proxy": "{{upstreams 5000}}",
              }),
              # Two site blocks on one container, one deliberately plain HTTP.
              facts(name="dual", internal_ports=[8000], labels={
                  "caddy_0": "one.example.com, two.example.com",
                  "caddy_0.reverse_proxy": "{{upstreams 8000}}",
                  "caddy_1": "http://plain.example.com",
                  "caddy_1.reverse_proxy": "{{upstreams 8000}}",
              }),
          ])
          got = {(h.domain, h.upstream_host, h.upstream_port, h.https) for h in caddy}
          assert got == {
              ("app.example.com", "app", 5000, True),
              ("one.example.com", "dual", 8000, True),
              ("two.example.com", "dual", 8000, True),
              ("plain.example.com", "dual", 8000, False),
          }, got

          # An unrecognised proxy, or none at all, must degrade to LAN links
          # rather than raising.
          assert discovery.proxy_hosts_for(None, None, []) == []

          # Tailscale CGNAT must be accepted even though modern Python does not
          # count 100.64.0.0/10 as private.
          assert discovery.acceptable_request_host("100.66.174.116")
          assert discovery.acceptable_request_host("192.168.2.100")
          assert not discovery.acceptable_request_host("8.8.8.8")
          assert not discovery.acceptable_request_host("not-an-ip")

          # Customisation store: the rules the edit UI depends on.
          import tempfile, pathlib
          import store as S

          path = pathlib.Path(tempfile.mkdtemp()) / "c.json"
          st = S.Store(path)

          st.create_category("Games")
          try:
              st.create_category("games")
              raise AssertionError("duplicate category accepted")
          except S.ValidationError:
              pass

          # Renaming carries members with it, including ones that were only
          # there by derivation -- otherwise they spring back on the next scan.
          st.set_app_fields("plex", {"name": "Plex Media", "category": "Games"})
          st.rename_category("Games", "Arcade", ["plex"])
          assert st.categories() == ["Arcade"]
          assert st.overrides()["apps"]["plex"]["category"] == "Arcade"

          # Deleting moves members to Uncategorized, which is itself immovable.
          st.delete_category("Arcade", ["plex"])
          assert st.overrides()["apps"]["plex"]["category"] == S.UNCATEGORIZED
          for op in (lambda: st.delete_category(S.UNCATEGORIZED, []),
                     lambda: st.rename_category(S.UNCATEGORIZED, "Junk", [])):
              try:
                  op()
                  raise AssertionError("Uncategorized was mutable")
              except S.ValidationError:
                  pass

          # Clearing a field resets it; clearing every field drops the entry.
          st.set_app_fields("plex", {"name": "", "category": ""})
          assert "plex" not in st.overrides()["apps"]

          # An icon name becomes a filename, so it is validated like one.
          try:
              st.set_app_fields("plex", {"icon": "../../etc/passwd"})
              raise AssertionError("path traversal accepted")
          except S.ValidationError:
              pass

          # Edits survive a restart, and a mangled file is not fatal.
          st.set_app_fields("sonarr", {"name": "TV"})
          assert S.Store(path).overrides()["apps"]["sonarr"]["name"] == "TV"
          path.write_text("{ not json")
          assert S.Store(path).categories() == []

          # Per-container memory: subtracting reclaimable page cache from the
          # raw cgroup usage figure, across both cgroup versions, and treating
          # a stopped container's empty {} the same as "not measurable" rather
          # than a crash.
          assert stats.humanize_bytes(None) == ""
          assert stats.humanize_bytes(512) == "512 B"
          assert stats.humanize_bytes(2_500_000_000) == "2.3 GB"

          v2_payload = {"memory_stats": {"usage": 1000, "stats": {"inactive_file": 300}}}
          assert stats._memory_from_stats(v2_payload) == 700
          v1_payload = {"memory_stats": {"usage": 1000, "stats": {"total_inactive_file": 100}}}
          assert stats._memory_from_stats(v1_payload) == 900
          no_cache_payload = {"memory_stats": {"usage": 1000, "stats": {}}}
          assert stats._memory_from_stats(no_cache_payload) == 1000
          assert stats._memory_from_stats({"memory_stats": {}}) is None, "a stopped container is not an error"
          assert stats._memory_from_stats({}) is None

          # Disk usage: parsed out of the same verbose payload `docker system df
          # -v` itself reads, through a fake client so no daemon is needed here.
          class _FakeAPI:
              def _url(self, path):
                  return path

              def _get(self, url, params=None):
                  assert params == {"verbose": "true"}, "must ask for the broken-out figures"
                  return "response"

              def _result(self, response, json):
                  return {
                      "Containers": [
                          {"Names": ["/radarr"], "SizeRw": 4096},
                          {"Names": ["/sonarr", "/sonarr-alias"], "SizeRw": 8192},
                      ],
                      "Volumes": [
                          {"Name": "media", "UsageData": {"Size": 500}},
                          {"Name": "not-yet-computed", "UsageData": {"Size": -1}},
                      ],
                  }

          class _FakeClient:
              api = _FakeAPI()

          container_rw, volumes = stats.read_disk_usage(_FakeClient())
          assert container_rw == {"radarr": 4096, "sonarr": 8192, "sonarr-alias": 8192}
          assert volumes == {"media": 500}, "a -1 size (not computed yet) must not be reported as zero"

          class _BrokenClient:
              class api:
                  @staticmethod
                  def _url(path): raise RuntimeError("daemon unreachable")

          assert stats.read_disk_usage(_BrokenClient()) == ({}, {}), "must degrade, not raise"

          # Mounts travel from a container's facts through to its card, so the
          # page can attribute a volume's size without a second daemon lookup.
          mounted = facts(name="radarr", mounts=[
              {"type": "bind", "source": "/data/radarr", "destination": "/config"},
              {"type": "volume", "source": "media", "destination": "/media"},
          ])
          built = discovery.build_apps([mounted], [], {"apps": {"radarr": {"port": 7878}}}, set())
          assert built[0].mounts == mounted.mounts

          # Appearance. These values are interpolated into a <style> block,
          # so anything that is not obviously a colour must be refused.
          look = S.Store(path)
          assert look.appearance()["theme"] == "system", "default must follow the device"
          assert look.appearance()["accent"] == ""

          look.set_appearance({"accent": "#a855f7", "background": "grid", "theme": "dark"})
          assert look.appearance()["accent"] == "#a855f7"
          assert look.appearance()["background"] == "grid"

          for bad in ({"accent": "red; } body { display: none"}, {"accent": "javascript:x"},
                      {"accent": "url(http://evil/x)"}, {"theme": "neon"},
                      {"background": "../../etc"}, {"background_url": "http://evil/x.png"},
                      {"nonsense": "1"}):
              try:
                  look.set_appearance(bad)
                  raise AssertionError("appearance accepted %r" % bad)
              except S.ValidationError:
                  pass

          # Clearing a field returns it to the default and drops it from disk,
          # so a reset leaves no residue to puzzle over later.
          look.set_appearance({"accent": "", "background": "plain", "theme": "system"})
          assert look.appearance()["accent"] == ""
          assert look.snapshot().get("appearance") == {}, look.snapshot().get("appearance")

          # Dim is clamped rather than rejected: a slider cannot send nonsense,
          # but an API caller can, and a black page is not a useful error.
          look.set_appearance({"background_dim": 500})
          assert look.appearance()["background_dim"] == 90

          # The stats switch. False has to survive the clear-to-reset rule that
          # treats an empty value as "back to the default" -- off is a choice,
          # not an absence, and losing it would silently start measuring again.
          assert look.appearance()["stats"] is True, "stats are on unless turned off"
          look.set_appearance({"stats": False})
          assert look.appearance()["stats"] is False
          assert look.snapshot()["appearance"]["stats"] is False, "off must reach the disk"
          look.set_appearance({"stats": "false"})
          assert look.appearance()["stats"] is False, "a curl sending a string means it too"
          look.set_appearance({"stats": True})
          assert look.appearance()["stats"] is True
          assert "stats" not in look.snapshot()["appearance"], "the default leaves no residue"

          # Host vitals. Every source is optional, so the reading must survive
          # a machine where none of them can be read.
          import stats as V

          v = V.Vitals(mem_used=8 * 1024**3, mem_total=32 * 1024**3,
                       disk_used=90, disk_total=100, uptime_seconds=90000, cpu_percent=12.0)
          assert v.mem_percent == 25 and v.mem_text == "8.0 / 32.0 GB", v.mem_text
          assert v.uptime_text == "1d 1h", v.uptime_text
          # Thresholds exist so a filling disk is noticeable before it is fatal.
          assert v.disk_level == "hot" and v.cpu_level == ""
          assert V.Vitals(mem_used=80, mem_total=100).mem_level == "warn"
          # Unknown sources must render as nothing, not as zero-of-zero.
          blank = V.Vitals()
          assert blank.mem_text == "" and blank.uptime_text == "" and blank.mem_percent == 0
          assert blank.as_dict()["cpu_percent"] is None

          # A rate needs two samples: the first reading must decline to guess.
          V._prev_cpu = None
          V.PROC = pathlib.Path(tempfile.mkdtemp())
          (V.PROC / "stat").write_text("cpu  100 0 100 800 0 0 0 0\n")
          assert V._cpu_percent() is None, "first sample cannot be a percentage"
          (V.PROC / "stat").write_text("cpu  200 0 200 1400 0 0 0 0\n")
          # 800 jiffies elapsed, 600 of them idle -> 25% busy. Held in a
          # variable because re-reading it in the message would sample again.
          busy = V._cpu_percent()
          assert busy == 25.0, busy
          # A counter reset (reboot) must not produce a negative or absurd value.
          (V.PROC / "stat").write_text("cpu  1 0 1 1 0 0 0 0\n")
          assert V._cpu_percent() is None
          # Missing /proc entirely is a supported state, not a crash.
          V.PROC = pathlib.Path("/nonexistent-proc")
          assert V.read_vitals().mem_total is None

          import server  # must import with no docker socket present
          assert server.app is not None

          # The page must render with no Docker at all, and the embed options
          # that Home Assistant relies on must actually take effect.
          client = server.app.test_client()
          plain = client.get("/")
          assert plain.status_code == 200
          assert b'data-theme' not in plain.data, "theme must follow the OS by default"

          page = plain.data.decode()
          assert 'id="search-count"' in page, "live search needs its result readout"
          assert 'id="theme-toggle"' in page, "the palette toggle is not an edit-mode feature"
          assert 'id="appearance-open"' in page, "accent/background must not require entering edit mode first"
          assert 'id="stats-toggle"' in page, "stats must be switchable without editing a compose file"
          assert "<style>" not in page, "the default look must add no inline style"

          # Switching stats off must stop the collection, not just the markup:
          # _stats_on is what the refresh loop consults before it asks the
          # daemon for anything, so it is the single fact both sides read.
          # (The store's own file lives at /config here, which the runner
          # cannot write, so the stored flag is stood in for rather than set.)
          assert server._stats_on() is True
          saved = server.store.appearance
          server.store.appearance = lambda: {**saved(), "stats": False}
          try:
              assert server._stats_on() is False, "the header switch must reach the refresh loop"
              off = client.get("/").data.decode()
              assert 'id="vitals"' not in off, "stats off must render no vitals strip"
              assert 'id="stats-toggle"' in off, "the way back on must stay on the page"
          finally:
              server.store.appearance = saved
          assert server._stats_on() is True

          embed = client.get("/?embed=1&compact=1&theme=light").data.decode()
          assert 'id="vitals"' not in embed, "compact must not spend rows on vitals"
          assert 'class="bg-plain embed compact"' in embed
          assert 'data-theme="light"' in embed
          assert 'id="edit-toggle"' not in embed, "embed must not offer editing"
          assert 'id="edit-toggle"' in client.get("/?embed=1&edit=1").data.decode()
          # An unknown theme is ignored rather than injected into the markup.
          assert 'data-theme' not in client.get("/?theme=neon").data.decode()

          # Favicon fallback: apple-touch first (largest icon most apps ship),
          # then declared icons, with /favicon.ico always tried last because
          # plenty of services serve one without ever declaring it.
          html = '''<html><head>
            <link rel="shortcut icon" href="/static/fav.png">
            <link rel="apple-touch-icon" sizes="180x180" href="assets/touch.png">
            <link rel="stylesheet" href="/x.css">
          </head></html>'''
          assert server._favicon_candidates("http://host:8080", html) == [
              "http://host:8080/assets/touch.png",
              "http://host:8080/static/fav.png",
              "http://host:8080/favicon.ico",
          ]
          # Missing or malformed markup must degrade, never raise.
          assert server._favicon_candidates("http://h:1", "") == ["http://h:1/favicon.ico"]
          assert server._favicon_candidates("http://h:1", "<link rel=icon>") == ["http://h:1/favicon.ico"]
          for _magic, suffix in server._IMAGE_MAGIC:
              assert suffix in server._MIME_BY_SUFFIX, suffix

          print("checks passed")
          PY

      - name: Validate the shell script
        run: bash -n nginx/apply.sh

  build:
    needs: check
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Check for Docker Hub credentials
        id: hub
        run: |
          if [ -n "${{ secrets.DOCKERHUB_TOKEN }}" ]; then
            echo "enabled=true" >> "$GITHUB_OUTPUT"
          else
            echo "enabled=false" >> "$GITHUB_OUTPUT"
            echo "::notice::DOCKERHUB_TOKEN not set -- publishing to GHCR only"
          fi

      - name: Log in to Docker Hub
        if: steps.hub.outputs.enabled == 'true'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Compute tags
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ${{ env.GHCR_IMAGE }}
            ${{ steps.hub.outputs.enabled == 'true' && env.DOCKERHUB_IMAGE || '' }}
          # latest tracks the default branch; branches and tags get their own.
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=sha,format=short
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: ${{ env.PLATFORMS }}
          # A pull request builds all four platforms but publishes nothing --
          # the point there is to prove the build still works everywhere.
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          provenance: false
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Summarise
        if: github.event_name != 'pull_request'
        run: |
          {
            echo "### Published"
            echo
            echo "Platforms: \`${{ env.PLATFORMS }}\`"
            echo
            echo '```'
            echo "${{ steps.meta.outputs.tags }}"
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"
